# Site structure: / (market hub) + /kl-check + /my/* + /th/* + /kl/ scorecards.
# NOTE: Cloudflare Pages evaluates _redirects BEFORE serving static assets, so a
# blanket /* rule would loop the home page and swallow every survivor. Do NOT add /*.
# Unmatched/old URLs are handled by /404.html (client-side redirect to /).

# Surviving legacy aliases (targets still exist):
/report           /my/report         301
/report.html      /my/report.html    301
/macro            /my/macro          301
/macro.html       /my/macro.html     301
/airbnb           /my/airbnb         301
/airbnb.html      /my/airbnb.html    301
