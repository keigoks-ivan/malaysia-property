# Site collapsed to: /kl-check (home) + /my/report + /my/macro + /kl/ scorecards.
# NOTE: Cloudflare Pages evaluates _redirects BEFORE serving static assets, so a
# blanket /* rule would loop /kl-check and swallow every survivor. Do NOT add /*.
# Unmatched/old URLs are handled by /404.html (client-side redirect to /kl-check).

# Surviving legacy aliases (targets still exist):
/report           /my/report         301
/report.html      /my/report.html    301
/macro            /my/macro          301
/macro.html       /my/macro.html     301
